node_modules/
dist/
*.log
.DS_Store
.env
.env.*
!.env.example

# Local / tooling — do not commit
.git/
**/.git
.claude/
.codex/
# Ignore .admin contents, but allow planning attic (tip factory/PLANNING-ATTIC.md §6)
.admin/*
!.admin/docs/
.admin/docs/*
!.admin/docs/plan-suites/
!.admin/docs/plan-suites/**
.codex-staging/


# TypeScript incremental build info — do not commit (causes non-reproducible builds)
**/tsconfig.tsbuildinfo

# Generated validation artifact — host-evidence trend report (machine-local)
pod-trends-report.html


# Local-only (maintainers ops archive — never committed to the public repo)
admin/

# Hive local overrides (machine tip path) — never commit
.hive/local.*
