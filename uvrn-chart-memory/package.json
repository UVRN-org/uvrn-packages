{
  "name": "@uvrn/chart-memory",
  "version": "5.0.1",
  "description": "Plain-default UVRN history → self-contained SVG/HTML time charts. Picture is never the proof — every artifact carries seriesRef + receipt/history refs.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "@uvrn/timeline": "^5.0.0"
  },
  "peerDependenciesMeta": {
    "@uvrn/timeline": {
      "optional": true
    }
  },
  "devDependencies": {
    "@uvrn/canon": "workspace:^",
    "@uvrn/drift": "workspace:^",
    "@uvrn/store-sqlite": "workspace:^",
    "@uvrn/test": "workspace:^",
    "@uvrn/timeline": "workspace:^",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "better-sqlite3": "^11.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/UVRN-org/uvrn-packages.git",
    "directory": "uvrn-chart-memory"
  },
  "publishConfig": {
    "access": "public"
  }
}
