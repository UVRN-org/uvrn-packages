/**
 * @uvrn/visual — receipt → self-contained plain HTML/SVG.
 * Picture is never the proof: every artifact carries receiptRef (wall 5).
 */

export * from './types';
export * from './render';
export * from './upload';
