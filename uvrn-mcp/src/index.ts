/**
 * @uvrn/mcp — library entry. No side effects on import.
 * Use createServer() / startServer() when you want to run the server.
 */

export { createServer, startServer } from './server.js';
export { logger } from './logger.js';
