{
  "name": "@uvrn/watch",
  "version": "1.0.0",
  "description": "Subscription and threshold alerts for UVRN claim monitoring",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "@uvrn/agent": ">=1.0.0",
    "@uvrn/drift": ">=1.0.0"
  },
  "devDependencies": {
    "@uvrn/agent": "workspace:*",
    "@uvrn/drift": "workspace:*",
    "@uvrn/test": "workspace:*",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "keywords": ["uvrn", "watch", "alerts", "threshold", "webhooks", "subscription"]
}
