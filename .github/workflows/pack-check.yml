# Pack + manifest check + contract test. Contract test runs in an isolated temp dir
# with only packed tarballs installed (no monorepo node_modules), ensuring default
# config and import behavior work for consumers.
name: Pack check

on:
  push:
    branches: [main, feature/updates]
  pull_request:
    branches: [main, feature/updates]

jobs:
  check-packed-manifests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Build
        run: pnpm run build

      - name: Test
        run: pnpm run test
        env:
          CI: 1

      - name: Pack all packages
        run: |
          for dir in uvrn-core uvrn-sdk uvrn-adapter uvrn-mcp uvrn-api uvrn-cli; do
            (cd "$dir" && pnpm pack)
          done

      - name: Check packed manifests for workspace: protocol
        run: node scripts/check-packed-manifests.js

      - name: Contract test (clean install from tarballs + smoke)
        run: pnpm run smoke:consumer
        timeout-minutes: 5
