{
  "name": "@uvrn/visual",
  "version": "5.0.1",
  "description": "Plain-default UVRN receipt → self-contained HTML/SVG views. Picture is never the proof — every artifact carries receiptRef.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "@uvrn/receipt": "^5.0.0"
  },
  "peerDependenciesMeta": {
    "@uvrn/receipt": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "@uvrn/receipt": "workspace:^",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/UVRN-org/uvrn-packages.git",
    "directory": "uvrn-visual"
  },
  "publishConfig": {
    "access": "public"
  }
}
